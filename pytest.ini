[pytest]
asyncio_mode = auto
markers =
    asyncio: mark test as async
python_files = test_*.py *_test.py
testpaths = tests
norecursedirs = _obsolete .git .venv build dist node_modules ui/web
filterwarnings =
    ignore::DeprecationWarning:pydantic.*
    ignore::pytest.PytestCollectionWarning

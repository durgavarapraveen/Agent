# AntiGravity Master Encryption Keys (AES-256-GCM)
# Generate a 32-byte key via: python -c "import os, base64; print(base64.b64encode(os.urandom(32)).decode())"

ENCRYPTION_KEY="ANTIGRAVITY_MASTER_KEY_32BYTES_LONG!"
ENCRYPTION_KEY_CURRENT="ANTIGRAVITY_MASTER_KEY_32BYTES_LONG!"
ENCRYPTION_KEY_PREVIOUS="ANTIGRAVITY_PREVIOUS_KEY_32BYTES_!"

# Retention & Governance Settings
RETAIN_SCANS_DAYS=90
RETAIN_PII_DAYS=365
AUDIT_LOG_PATH="data/audit.log"

# Censys Platform API (Personal Access Token)
CENSYS_PAT=your_censys_personal_access_token

# Free Tier Rate-Limit Throttling Mode (DeepSeek is paid; all other APIs operate under free-tier limits)
FREE_TIER_MODE=true

